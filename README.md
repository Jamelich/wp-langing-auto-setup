# 🚀 WordPress Auto-Setup Script

**Версия скрипта:** 2.1-PLUGINS-UPDATE  
**Репозиторий:** [https://github.com/Jamelich/wp-langing-auto-setup](https://github.com/Jamelich/wp-langing-auto-setup)

Автоматический скрипт для быстрого развёртывания чистого WordPress, установки необходимых плагинов и клонирования шаблонной темы прямо на хостинг.

## 🎯 Что делает этот скрипт

Одной командой подготавливает полностью рабочую среду для нового сайта на WordPress:

1.  **📦 Устанавливает свежий WordPress** — скачивает и распаковывает последнюю стабильную версию с wordpress.org.
2.  **🔌 Устанавливает набор плагинов** — предустановленный список часто используемых плагинов.
3.  **⚙️ Устанавливает Carbon Fields** — загружает популярный фреймворк полей напрямую с GitHub.
4.  **🎨 Клонирует шаблонную тему** — загружает вашу тему `esalanding` из репозитория на GitHub.

## 📋 Установленные плагины (версия 2.1)

### Из официального репозитория WordPress:
- **Classic Editor** (`classic-editor`) — Возвращает классический редактор WordPress.
- **Classic Widgets** (`classic-widgets`) — Классические виджеты до WordPress 5.8.
- **Cyr-To-Lat** (`cyr2lat`) — Транслитерация кириллических URL.
- **Favicon by RealFaviconGenerator** (`favicon-by-realfavicongenerator`) — Генератор фавиконок.
- **Яндекс Метрика** (`yandex-metrica`) — Интеграция с Яндекс.Метрикой.

### С GitHub:
- **Carbon Fields** — Фреймворк кастомных полей для разработчиков (устанавливается напрямую с GitHub).

## 🛠️ Быстрый старт

Чтобы установить WordPress с плагинами и темой в **пустую папку** на вашем хостинге, выполните всего одну команду в терминале (например, в WinSCP):

```bash
rm -f setup.sh && wget -q https://raw.githubusercontent.com/Jamelich/wp-langing-auto-setup/main/setup.sh?$(date +%s) -O setup.sh && bash setup.sh
```

После успешного выполнения в текущей папке появится структура WordPress с установленными плагинами и темой.

## ⚙️ Настройка под свои нужды

Вы можете легко изменить список плагинов, отредактировав файл `setup.sh` в репозитории. Найдите в скрипте массив `PLUGINS`:

```bash
PLUGINS=(
    "classic-editor"
    "classic-widgets"
    "cyr2lat"
    # Добавьте или удалите слаги плагинов здесь
)
```

---

**🚀 Одна команда — готовый сайт с вашим набором инструментов!**
